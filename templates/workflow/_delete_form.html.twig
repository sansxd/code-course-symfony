<form method="post" action="{{ path('app_workflow_delete', {'id': workflow.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workflow.id) }}">
    <button class="btn">Delete</button>
</form>
